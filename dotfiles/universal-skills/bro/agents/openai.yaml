interface:
  display_name: "Bro"
  short_description: "Rephrase the previous response in plain language"
  default_prompt: "Use $bro to restate my last response in plain English."

policy:
  allow_implicit_invocation: false
