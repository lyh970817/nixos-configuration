interface:
  display_name: "Nix Env Setup"
  short_description: "Fix a missing project tool via shell.nix and direnv"
  default_prompt: "Use $nix-environment-setup to add the missing tool or variable to this project's shell.nix and .envrc."

policy:
  allow_implicit_invocation: true
