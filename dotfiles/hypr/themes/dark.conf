general {
    gaps_in = 8
    gaps_out = 12
    border_size = 1
    col.active_border = rgba(3D8E48ff)
    col.inactive_border = rgba(15261Aff)
}
misc {
    background_color = 0x050806
}

decoration {
    rounding = 4
    # Panel shader. The amber-tinted bloom and black lift that made the old
    # one contaminate green colour measurements are both gone: the lift was
    # removed outright and the remaining tints follow the phosphor. Note that
    # theming.nix sets this live on every mode switch, so this line only
    # governs a cold start.
    screen_shader = ~/.config/hypr/shaders/panel.glsl

    # Change transparency of focused and unfocused windows
    inactive_opacity = 1
    active_opacity = 1

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = false
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# Startup cursor, so a dark-mode login matches switch-dark without waiting for
# it. No hyprcursor variant of this theme is built, so HYPRCURSOR_THEME does not
# resolve and Hyprland falls back to the XCursor of the same name -- exactly
# what Adwaita did here before.
env = HYPRCURSOR_THEME,Bibata-Original-VT220
env = XCURSOR_THEME,Bibata-Original-VT220
env = XCURSOR_SIZE,22

# Brown-black CRT glass background for startup.
exec-once = pkill swaybg || true; setsid -f swaybg -c 050806
# exec-once = $TERMINAL --app-id foot-main tmux new-session -A -s main
exec-once = hyprctl setcursor Bibata-Original-VT220 22
