interface:
  display_name: "Session Handoff"
  short_description: "Continue work in another Codex session"
  default_prompt: "Use $session-handoff to continue this work in a fresh Codex session."

policy:
  allow_implicit_invocation: false
