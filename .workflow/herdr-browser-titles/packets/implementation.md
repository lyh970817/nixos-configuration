# Implementation packet

Owner: `implement_dynamic_titles`

Implement the browser bindings, title coordinator, managed Codex enqueue hooks, user service, and fake protocol/API regression coverage as one coherent state-machine change. Never operate on the active Herdr session or call the real DashScope endpoint.
