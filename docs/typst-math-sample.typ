= Typst Math Smoke Note

This file is a small note to open with Neovim after the Typst setup is installed.

Use `:TypstPreview` to start the preview. Typstar snippets include `kk` for
inline math and `dm` for display math.

$ sum_(i=1)^n i = (n (n + 1)) / 2 $

$ integral_0^1 x^2 dif x = 1 / 3 $
